extends layout

block content
  h1= title
  p Welcome to #{title}
  h2 --Messages--
  each message in messages
    h3= message.user
    h4= message.added
    p= message.text
  a(href="/new") 
    button New Message